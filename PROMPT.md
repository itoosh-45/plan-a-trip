# פרומפט אב — "תכנון טיול ותקציב" (PWA)

> העתק את כל הבלוק הזה לתוך Claude Code בתחילת הפרויקט.

---

## Objective — מה בונים

בנה אפליקציית **PWA** (Progressive Web App) בעברית, RTL, בשם **"תכנון טיול ותקציב"**, שמותקנת למסך הבית של אייפון דרך Safari → Share → "הוסף למסך הבית", ופועלת **במלואה אופליין**.

האפליקציה עושה שלושה דברים:
1. **תכנון מסלול** — איפה אני נמצא בכל תאריך, ומה קורה באותו יום (אטרקציות / לינה / מסעדות / טיסות / מעברים / נסיעות / מפגשים).
2. **תכנון תקציב מראש** — טיסות, מלונות, Airbnb, Booking, אטרקציות, ציוד; גם מה שכבר שולם מראש וגם מה שמתוכנן להוצאה בהמשך.
3. **מעקב הוצאות בפועל בזמן הטיול** — כמה מזומן יצא, ומה קרה בעסקאות הגדולות (טיסות, לינה, מעברים, השכרת רכב, קורסים).

---

## החלטה טכנולוגית — ענה לי על זה לפני שאתה כותב שורת קוד

הנחת המוצא שלי: אין לי דרך להוציא APK לאייפון, ואני לא רוצה להסתבך עם App Store, ולכן נבנה אתר HTML שמוצמד למסך הבית.

**אשר או תקן אותי.** תן לי השוואה קצרה (טבלה, לא מאמר) בין:
- **PWA** מוצמד למסך הבית
- **Capacitor / Xcode + חשבון מפתח** ($99 לשנה)
- **TestFlight**
- **Sideload** דרך AltStore / SideStore

לכל אחד: מה עולה, מה הכאב, ומה נשבר. אם PWA היא הבחירה הנכונה — אמור זאת מפורשות וציין **מה המחיר של הבחירה הזו באייפון**, לפחות: אין התראות Push אמינות, אחסון מקומי עלול להימחק אחרי תקופת אי-שימוש (ולכן הגיבוי הוא חובה ולא נחמד-שיהיה), אין רקע/סנכרון אוטומטי, אין גישה לאנשי קשר/קבצים כמו באפליקציה נייטיב.

**אל תתחיל לבנות עד שאישרתי את ההכרעה הזו.**

---

## Stack — קבוע, אל תחליף בלי לשאול

- HTML + CSS + JavaScript. **קובץ יחיד או מעט קבצים סטטיים**, בלי שלב build, בלי npm, בלי framework כבד.
- Tailwind (CDN) לעיצוב + משתני CSS לטוקנים.
- **IndexedDB** לאחסון (לא localStorage — כי אנחנו שומרים היסטוריית טיולים שלמה).
- Service Worker + `manifest.json` לאופליין מלא והתקנה למסך הבית.
- ספריות מותרות בלבד: **SheetJS (xlsx)** לאקסל, **Chart.js** לגרפים. כל ספרייה נוספת — **תשאל אותי קודם**.
- אין שרת. אין backend. אין חשבון משתמש.

---

## Data Model — הגדר אותו איתי לפני המסכים

הצע לי סכימה ותן לי לאשר. הבסיס:

- **Trip** — שם, יעד/יעדים, תאריך התחלה, תאריך סיום, מטבע ראשי, תקציב כולל, סטטוס (מתוכנן / פעיל / הסתיים).
- **Day** — תאריך, עיר/מיקום, לינה של אותו לילה, הערות.
- **Item** (פריט במסלול) — סוג (אטרקציה / לינה / מסעדה / טיסה / מעבר / נסיעה / מפגש / אחר), כותרת, תאריך + שעה, מיקום, קישור/מספר הזמנה, סטטוס תשלום, עלות + מטבע.
- **Expense** (הוצאה) — קטגוריה, סכום, מטבע, שער המרה שנשמר **ברגע ההוצאה**, אמצעי תשלום (מזומן / אשראי / העברה), תאריך, קישור אופציונלי ל-Item, הערה.
- **Budget** — תקציב לפי קטגוריה, מול בפועל.
- **Settings** — מטבע בית (₪), שערי חליפין אחרונים + חותמת זמן שליפה.

חוקים שאסור לשבור:
- כל סכום נשמר **גם במטבע המקורי וגם בשקל**, לפי השער שהיה תקף ברגע ההזנה. שינוי שער בעתיד **לא** משנה הוצאות עבר.
- מחיקה של טיול מבקשת אישור כפול.
- כל רשומה נושאת `id`, `createdAt`, `updatedAt`.

---

## Features — מה חייב להיות

### מסכים
1. **בית / רשימת טיולים** — ניהול כמה טיולים במקביל, מעבר ביניהם, יצירת טיול חדש.
2. **תכנון טיול** — ציר זמן לפי תאריכים. לכל יום: מיקום, לינה, ורשימת פריטים. הוספה/עריכה/גרירה בין ימים.
3. **תקציב** — הגדרת תקציב מראש לכל קטגוריה, והצגת מתוכנן מול בפועל מול יתרה.
4. **הוצאות** — הזנה מהירה בזמן הטיול, סינון לפי קטגוריה/תאריך/אמצעי תשלום, מעקב נפרד אחרי **מזומן**.
5. **סיכום** — כל הנתונים במקום אחד: סה"כ מתוכנן, סה"כ שולם, יתרה, **גרף עוגה לפי קטגוריות**, פילוח מזומן מול אשראי, העסקאות הגדולות.
6. **הגדרות** — מטבע, שערי חליפין, ייצוא/ייבוא אקסל, גיבוי לגוגל דרייב, מחיקת נתונים.

### מטבעות
- תמיכה בכל מטבע זר, המרה לשקל.
- עדכון שערים אוטומטי **כשיש אינטרנט**, ושמירה מקומית לשימוש אופליין.
- הצג תמיד מתי השער עודכן לאחרונה. באופליין — הצג את השער השמור ואת גילו, ואל תיכשל.
- הצע לי 2-3 מקורות שער חינמיים וללא מפתח, ותן לי לבחור. **אל תשים מפתח API בקוד.**

### ייצוא/ייבוא אקסל
- **ייצוא**: קובץ `.xlsx` בפורמט קבוע ומוגדר מראש — גיליונות נפרדים (סיכום / מסלול / תקציב / הוצאות), כותרות מודגשות, רוחב עמודות מותאם, צבעי רקע לכותרות לפי פלטת הצבעים למטה, פורמט מטבע ותאריך נכון, שורות סיכום מודגשות, **RTL**.
- **ייבוא**: העלאת קובץ באותו פורמט חזרה לאפליקציה, זיהוי הגיליונות, ולידציה, ו**תצוגה מקדימה של מה ייובא לפני שמירה**. קובץ לא תקין → הודעת שגיאה ברורה בעברית שאומרת מה בדיוק לא תקין, בלי לאבד נתונים קיימים.
- הפורמט חייב להיות **סימטרי**: כל מה שיוצא — חוזר.

### אופליין וגיבוי
- אופליין מלא. אין מסך שמתפוצץ בלי רשת.
- גיבוי ידני ל-**Google Drive** (קובץ JSON מלא) + שחזור ממנו. הגיבוי הוא באחריות המשתמש ובלחיצת כפתור, לא אוטומטי.
- הסבר לי בפשטות מה נדרש כדי שגוגל דרייב יעבוד, ואם זה מסובך מדי — הצע חלופה (ייצוא קובץ גיבוי לשיתוף דרך iOS Share Sheet) ותן לי לבחור.

---

## Design System — מחייב, אל תמציא ערכים חדשים

### פלטת צבעים
| תפקיד | ערך | משתנה CSS |
|---|---|---|
| רקע האפליקציה | `#FCFCFC` | `--color-bg` |
| משטח כרטיס | `#FFFFFF` | `--color-surface` |
| משטח מוגבה / כפתור משני | `#F2F4F6` | `--color-surface-2` |
| שדה טופס | `#F5F7F8` | `--color-input` |
| טקסט ראשי | `#000000` | `--color-text` |
| טקסט משני | `#5A6672` | `--color-text-dim` |
| משני / הדגשות | `#06BCC1` | `--color-accent` |
| הדגשה שלישית | `#EE4266` | `--color-highlight` |
| קו הפרדה | `rgba(0,0,0,0.10)` | `--color-border` |
| מילוי עדין | `rgba(0,0,0,0.06)` | `--color-hairline` |

**צבעי סטטוס — נפרדים לחלוטין מהפלטה שלמעלה, ומשמשים אך ורק לסטטוס:**

| שגיאה / מחיקה | `#D92D20` | `--color-danger` |
| הצלחה / הכנסה | `#0E9F6E` | `--color-success` |
| אזהרה | `#D97706` | `--color-warning` |

### צורה ומרווח
- רדיוס כרטיס `16px` (`rounded-card`). רדיוס פקד/כפתור/שדה `12px` (`rounded-xl`).
- **צל אחד בלבד** לכרטיסים: `shadow-stamp` = `0 1px 2px rgba(16,24,40,0.04), 0 6px 16px -4px rgba(16,24,40,0.08)`. אל תמציא צללים חדשים ואל תשתמש בצללים כהים.
- היררכיה נוצרת מ**גובה משטח + קו הפרדה**, לא מצל כבד: רקע `#FCFCFC` → כרטיס `#FFFFFF` עם `border-line`.
- ריפוד בכרטיס `p-4`. מרווח בין כרטיסים `mt-3`. מרווח צד של מסך `px-4`.
- כל מסך מסתיים ב-`pb-36` כדי לא להסתתר מאחורי הניווט התחתון.

### רכיבים
- **כפתור ראשי:** רקע `#EE4266`, טקסט לבן, `font-bold`, `rounded-xl`, `py-3`.
- **כפתור משני:** רקע `#06BCC1`, טקסט לבן.
- **כפתור שלישוני:** רקע `bg-surface`, מסגרת `border-line`, טקסט שחור.
- **כפתור הרסני:** טקסט וגבול `#D92D20`, רקע שקוף.
- **אייקונים:** SVG וקטורי בלבד מתוך אובייקט `ICONS`, `stroke="currentColor"`, `stroke-width="1.8"`. **אין אימוג'י בשום מקום בממשק.** אייקון בודד ללא רקע — `.icon-btn` בצבע `--color-accent`, בלי עיגול מאחוריו.
- **צ'יפ נבחר:** רקע `bg-accent/10`, מסגרת `border-accent`, טקסט `text-accent`.
- **שדות טופס:** רקע `--color-input`, מסגרת `--color-border`, `:focus` עם `outline: 2px solid var(--color-accent)`.

### דרישות נוספות
- עברית מלאה, `dir="rtl"`, טיפוגרפיה שקטה, ללא עומס.
- מיטוב לאייפון: `safe-area-inset` למעלה ולמטה, אזורי מגע 44px לפחות, בלי zoom בלחיצה על שדה, בלי גלילה אופקית.
- מטרת המראה: **מקצועי, נקי, אלגנטי.** אם אתה מזהה מקום שבו העיצוב יוצא רועש או עמוס — עצור ותציע לי חלופה.

---

## Scope — גבולות

- עבוד רק בתיקיית הפרויקט `C:\Users\USER\PLAN A TRIP`.
- אל תיצור קבצים מחוץ לתיקייה הזו.
- אל תוסיף פיצ'רים, מסכים, הפשטות או קבצים שלא ביקשתי.
- אל תשנה את פלטת הצבעים, הרדיוסים או הצל.
- אין credentials, אין מפתחות API בקוד.

---

## Action Boundaries — כללי עבודה מולי

- **שאל אותי לפני כל החלטת עיצוב, לפני כל פיצ'ר חדש, ולפני כל תוספת או שינוי** — לאורך כל הדרך, לא רק בהתחלה. הצג אפשרויות + ההמלצה שלך, וחכה לתשובה.
- קידום שלב לשלב, עם **נקודת אישור בסוף כל שלב**. אל תרוץ קדימה.
- מותר לך לקרוא קבצים, לכתוב קוד בתוך התיקייה, ולהריץ בדיקות — בלי לשאול.
- **עצור ושאל לפני:** מחיקת קבצים, התקנת ספרייה, שינוי הסכימה של הנתונים אחרי שאושרה, כל דבר שמוציא מידע החוצה.
- כל טענה על "עובד" חייבת להישען על תוצאה אמיתית שראית — לא על הנחה.

---

## תוכנית עבודה — שלבים לפי הסדר

לכל שלב: תאר לי מה אתה עומד לעשות, בצע, הצג תוצאה, **בקש אישור, ורק אז המשך**.

| # | שלב | תוצר |
|---|---|---|
| 0 | הכרעת PWA מול חלופות | טבלת השוואה + המלצה + אישור שלי |
| 1 | מודל נתונים | סכימה כתובה + אישור שלי |
| 2 | תרשים זרימה | דיאגרמה של מסכים, מצבי נתונים, אופליין, ייצוא/ייבוא, גיבוי |
| 3 | שלד + Design System | `index.html`, טוקנים, `ICONS`, ניווט תחתון, מסכים ריקים |
| 4 | שכבת נתונים | IndexedDB, CRUD, ריבוי טיולים |
| 5 | מסך תכנון טיול | ציר זמן לפי ימים + פריטים |
| 6 | תקציב + הוצאות | תקציב מראש, הזנת הוצאות, מעקב מזומן |
| 7 | מטבעות | המרה, שמירת שער היסטורי, עדכון מקוון |
| 8 | מסך סיכום | סטטיסטיקות + גרף עוגה |
| 9 | אקסל | ייצוא מעוצב + ייבוא עם ולידציה ותצוגה מקדימה |
| 10 | אופליין + התקנה | Service Worker, manifest, אייקונים |
| 11 | גיבוי גוגל דרייב | גיבוי + שחזור |
| 12 | QA ובדיקות קצה | דוח בדיקות מלא |
| 13 | ליטוש עיצובי | מעבר אחרון על עקביות ויזואלית |

---

## תרשים זרימה — נדרש בשלב 2

הפק תרשים (Mermaid) שמראה:
- מסלול המשתמש בין המסכים.
- זרימת הנתונים: הזנה → IndexedDB → חישוב → תצוגה.
- זרימת המרת המטבע ומתי נשמר שער.
- זרימת ייצוא/ייבוא אקסל כולל ענף הכישלון.
- זרימת מקוון מול אופליין, וזרימת הגיבוי.

---

## QA ובדיקות קצה — נדרש בשלב 12

הרץ בעצמך ודווח לי תוצאות אמיתיות, לפחות:

**נתונים:** טיול בלי ימים · טיול של יום אחד · טיול חוצה שנה · שני טיולים חופפים בתאריכים · מחיקת טיול עם הוצאות · 500+ הוצאות (ביצועים).

**כספים:** סכום 0 · סכום שלילי · עשרוניות · מטבע בלי שער שמור · שינוי שער אחרי הזנה (הוצאות עבר חייבות להישאר קפואות) · חריגה מהתקציב · תקציב שלא הוגדר.

**אקסל:** ייצוא → ייבוא של אותו קובץ → הנתונים זהים · קובץ ריק · קובץ עם עמודות חסרות · קובץ שנערך ידנית · קובץ שאינו xlsx · קובץ ענק.

**אופליין:** ניתוק רשת באמצע פעולה · טעינה ראשונה אופליין · חזרה לרשת ועדכון שערים · אחסון מלא.

**ממשק:** מסך iPhone SE ועד Pro Max · טקסט ארוך מאוד בשם פריט · RTL מול מספרים ומטבעות לועזיים · תצוגה אופקית · מקלדת שמכסה שדה.

לכל בדיקה: מה נבדק, מה קרה, עבר או נכשל. תקן כשל, והרץ שוב.

---

## Acceptance Criteria — סיימנו כאשר

- [ ] האפליקציה נטענת מהאייקון במסך הבית של אייפון, במסך מלא, בלי שורת כתובת.
- [ ] כל הפונקציונליות עובדת ב-Airplane Mode.
- [ ] אפשר לנהל לפחות שני טיולים במקביל בלי דליפת נתונים ביניהם.
- [ ] מסך התכנון מציג כל תאריך עם המיקום והפריטים שלו.
- [ ] תקציב מוגדר מראש מוצג מול הוצאות בפועל, כולל יתרה.
- [ ] הוצאה במטבע זר נשמרת עם השער של רגע ההזנה, ולא משתנה בדיעבד.
- [ ] גרף העוגה משקף נכון את חלוקת ההוצאות לפי קטגוריה.
- [ ] ייצוא לאקסל מפיק קובץ מעוצב, וייבוא שלו חזרה משחזר את אותם נתונים בדיוק.
- [ ] גיבוי ושחזור עובדים מקצה לקצה.
- [ ] העיצוב תואם לפלטה ולמידות שהוגדרו, ואין אימוג'י בשום מקום בממשק.
- [ ] דוח QA מלא הוגש, וכל כשל שנמצא — תוקן.

---

## Progress Evidence

בסוף כל שלב דווח: **✅ מה הושלם · מה נבדק בפועל · מה נשאר · ומה השאלה הבאה שאתה צריך ממני.**
